name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-static.yml@master
    with:
      host: demos.codective.ir
      base-href: /
    secrets: inherit
