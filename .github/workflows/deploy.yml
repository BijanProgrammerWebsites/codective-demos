name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-nextjs-export.yml@master
    with:
      domain: demos.codective.ir
      base-href: /
    secrets: inherit
